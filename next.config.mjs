/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "m.media-amazon.com"], // Add the hostname here
  },
};

export default nextConfig;
